@model SchroniskoWebowka.Models.Pies
@{
    string nowaDataStr = "";
    if (Model.PiesDataUrodzenia != null)
    {
        DateTime staraData = (DateTime)Model.PiesDataUrodzenia;
         nowaDataStr = staraData.ToString("yyyy-MM-dd");
    }
    else{
		 nowaDataStr = "Brak daty";
	}
}


<div class="container mt-5">
    <div class="row">
        <div class="col-md-6">
            @if (Model.PiesZdjecie != null)
            {
                <img src="data:image/png;base64,@Convert.ToBase64String(Model.PiesZdjecie)" alt="@Model.PiesNazwa" class="img-fluid">
            }
            else
            {
                <p>Brak zdjęcia dla tego psa</p>
            }

        </div>

        <div class="col-md-6">

            <h2>@Model.PiesNazwa</h2>
            <p>@Model.PiesOpis</p>

            <ul class="list-group">
                <li class="list-group-item"><strong>Rasa:</strong> @Model.Rasa.RasaNazwa</li>
                <li class="list-group-item"><strong>Wiek:</strong> @Model.Wiek.WiekNazwa</li>
                <li class="list-group-item"><strong>Charakter:</strong> @Model.Charakter.CharakterNazwa</li>
                <li class="list-group-item"><strong>Strefa:</strong> @Model.Strefa.StrefaNazwa</li>
                <li class="list-group-item"><strong>Data przyjęcia:</strong> @nowaDataStr</li>
                <li class="list-group-item"><strong>Opis sierści:</strong> @Model.PiesKolorWzorSiersci</li>

            </ul>
        </div>
    </div>
</div>
